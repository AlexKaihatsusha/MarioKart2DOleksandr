using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.InputSystem;


public class InitiateInputForPlayers : MonoBehaviour
{
    /*public List<GameObject> playerPrefabs;
    
    private void Awake()
    {
        var p1 = PlayerInput.Instantiate(playerPrefabs[0], controlScheme: "Keyboard", pairWithDevice:Keyboard.current);
        var p2 = PlayerInput.Instantiate(playerPrefabs[1], controlScheme: "KeyboardRight", pairWithDevice:Keyboard.current);
    }*/
}
